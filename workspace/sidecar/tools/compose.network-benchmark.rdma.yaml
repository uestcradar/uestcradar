version: "2.4"

services:
  sidecar-test:
    environment:
      SIDECAR_DOWNSTREAM_DATA_PATH: strict-rdma
      UCX_PROTO_INFO: "y"
    devices:
      - /dev/infiniband:/dev/infiniband
    cap_add:
      - IPC_LOCK
    ulimits:
      memlock:
        soft: -1
        hard: -1
